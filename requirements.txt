# Glass Atrium — Python dependency DECLARATION set.
#
# This file declares the runtime package set and has no runtime consumer. The
# EXECUTED set is the GA_PYTHON_IMPORTS array in lib/ga-deps.sh, which the
# dependency-bootstrap preflight installs. test/python-deps-parity.bats binds
# the two, so adding a package means editing BOTH — the suite stays red until
# it does.
#
# Two psycopg generations are intentionally BOTH present — they are NOT redundant:
#   * psycopg2-binary — psycopg v2, imported as `import psycopg2`
#                       (hooks/_pg-write.py uses the v2 API).
#   * psycopg         — psycopg v3, imported as `import psycopg`
#                       (the autoagent / monitor daemons use the v3 API).
# Removing either breaks one consumer; keep both pinned.
psycopg
psycopg2-binary
PyYAML
