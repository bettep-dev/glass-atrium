# Glass Atrium — CI-only Python test dependencies.
#
# Deliberately SEPARATE from requirements.txt: that file is the DECLARATION set
# for the end-user runtime packages (the executed set is GA_PYTHON_IMPORTS in
# lib/ga-deps.sh), so a test-only package must never ship into a user install.
# Consumed only by the `test-python-pytest` CI job:
#   python3 -m pip install -r requirements-dev.txt
#
# The scripts/test suites import stdlib + pytest + in-repo packages only
# (psycopg is a degrade-on-absence local import), so pytest is the whole set.
pytest
