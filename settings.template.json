{
  "//": [
    "Glass Atrium ~/.claude/settings.json PERMISSIONS template (DF-22 reference — NOT auto-applied).",
    "DEFECT: an rm NEGATION placed inside permissions.allow — e.g. \"!Bash(rm:*)\" — is likely INERT.",
    "  Claude Code's permission layer denies via the permissions.deny array; an allow-list negation is",
    "  not the documented deny mechanism, so \"Bash(*)\" + \"!Bash(rm:*)\" in allow does NOT reliably block",
    "  rm at the permission layer (the block-dangerous-commands.sh hook is a SEPARATE, later gate).",
    "FIX: declare rm rules in permissions.deny (below). deny is evaluated with precedence over allow.",
    "APPLY: merge ONLY the \"permissions\" object into your existing ~/.claude/settings.json (never wipe",
    "  other keys). wire_hooks (lib/ga-config-hooks.sh) never touches permissions, so this stays manual.",
    "LIVE-VERIFY (see the \"live_verify\" key): confirm rm is blocked at the PERMISSION layer, not merely",
    "  by the dangerous-command hook.",
    "HOOKS: the \"hooks\" block below documents the edit-time syntax gate (validate-edit-syntax.sh), a",
    "  PreToolUse(Write|Edit) advisory that runs bash -n / node --check / py_compile by extension allowlist.",
    "  It is advisory-first (warns on a syntax error; set SYNTAX_GATE_BLOCK=1 to block after burn-in) and",
    "  exempts templates. wire_hooks (from the ga-env.sh EXPECTED_HOOK_BINDINGS SoT) wires it automatically",
    "  with an absolute command path; this block is the manual-reference form.",
    "  enforce-harness-critical.sh (two rows: Write|Edit + Bash) is the harness-critical file gate — it",
    "  blocks agent_id-INDEPENDENT writes to live settings.json/settings.local.json, live hook dirs, and",
    "  agents/*.md frontmatter identity keys {name, tools, scope} (model excluded; new agents/*.md Write",
    "  blocked — creation routes through the agent_lifecycle CLI). Grant: HARNESS_PROTECTION_APPROVE=1 in",
    "  the LAUNCH environment only (an in-session export never reaches the hook env).",
    "INTERPRETER NEGATIONS (T21): the deny rows Bash(bash -c:*) / Bash(sh -c:*) / Bash(zsh -c:*) /",
    "  Bash(eval:*) close a command-prefix-matcher BYPASS — an inline shell re-parse (bash -c 'rm -rf ~')",
    "  would otherwise smuggle a denied verb past Bash(rm:*), whose match anchors on the LEADING token only.",
    "  These forms ARE expressible as command-prefix matchers (leading-token anchored) and have no",
    "  legitimate agent use — an agent runs commands through the Bash tool directly, never wrapped.",
    "  RESIDUAL (deliberately NOT denied): a LANGUAGE-interpreter inline form (python3 -c, node -e, perl -e)",
    "  is an equivalent bypass vector but collides with legitimate portability use (e.g. python3 -c date",
    "  math on BSD/GNU divergence), so it stays for the block-dangerous-commands.sh + enforce-harness-",
    "  critical.sh backstops rather than a blanket permission-layer deny.",
    "REDIRECT INEXPRESSIBILITY (T21): 'a redirect INTO a harness path' (echo x > ~/.claude/hooks/y) is",
    "  INEXPRESSIBLE as a Bash(<prefix>:*) deny — the > operator and its path target appear anywhere in the",
    "  command, not as a leading prefix the matcher anchors on. That pattern therefore lives ONLY in",
    "  enforce-harness-critical.sh's redirect regex (DETECT_PY REDIR_RE + PROT_RE), never here."
  ],
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "$HOME/.glass-atrium/hooks/validate-edit-syntax.sh"
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "$HOME/.glass-atrium/hooks/enforce-harness-critical.sh"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "$HOME/.glass-atrium/hooks/enforce-harness-critical.sh"
          }
        ]
      }
    ]
  },
  "permissions": {
    "defaultMode": "auto",
    "allow": [
      "Bash(*)"
    ],
    "deny": [
      "Bash(rm:*)",
      "Bash(rm -rf:*)",
      "Bash(bash -c:*)",
      "Bash(sh -c:*)",
      "Bash(zsh -c:*)",
      "Bash(eval:*)"
    ]
  },
  "live_verify": [
    "1. Apply the permissions block above to ~/.claude/settings.json, then start a fresh 'claude' session.",
    "2. Request an rm command, e.g. Bash: rm /tmp/ga-df22-probe",
    "3. EXPECT: the call is DENIED at the permission layer (permission prompt / auto-deny), BEFORE any",
    "   hook fires. A pass = rm cannot execute. Contrast with the pre-fix state, where '!Bash(rm:*)' sat",
    "   inside permissions.allow and rm was NOT permission-denied (only the hook, if present, caught it).",
    "4. Positive control: Bash: ls /tmp — MUST still be permitted (deny is scoped to rm only)."
  ]
}
